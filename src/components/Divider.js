import React from 'react';
import {View} from 'react-native';

const Divider = () => {
    return <View style={{

        backgroundColor: 'red',
        height: 1,
        marginTop: 15,
        marginBottom: 15,
        borderWidth: 1,
        borderColor: 'white',
        borderStyle: 'dashed'
    }}/>;
};
export default Divider;
